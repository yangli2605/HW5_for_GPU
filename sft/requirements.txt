# 核心依赖
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0

# LoRA 和高效微调
peft>=0.7.0
bitsandbytes>=0.41.0

# TRL (Transformer Reinforcement Learning)
trl>=0.7.0

# 可选: DeepSpeed (用于全量微调加速)
# deepspeed>=0.12.0

# 工具库
pandas>=2.0.0
numpy>=1.24.0
tensorboard>=2.14.0

# 其他
sentencepiece>=0.1.99
protobuf>=3.20.0
einops>=0.7.0